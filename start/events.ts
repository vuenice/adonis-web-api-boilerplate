/*
|--------------------------------------------------------------------------
| Events (optional)
|--------------------------------------------------------------------------
|
| Register application-wide event listeners here, e.g.:
|   emitter.on(SomeEvent, [ListenerClass, 'handle'])
|
*/

export {}
