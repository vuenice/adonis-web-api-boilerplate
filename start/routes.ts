/*
|--------------------------------------------------------------------------
| Routes entry
|--------------------------------------------------------------------------
|
| Convention: anything that should return JSON for errors and clients must
| live under the /api prefix (see app/exceptions/handler.ts). Browser pages
| use plain paths and the web route group in start/routes/web.ts.
|
*/

import './routes/web.js'
import './routes/api.js'
